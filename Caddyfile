{$API_DOMAIN} {
	# The real upload cap: FastAPI spools a whole body before /upload can reject
	# it. UPLOAD_MAX_MB (25) plus multipart overhead.
	request_body {
		max_size 30MB
	}
	reverse_proxy api:8000
}
